ss: vs_main.sampler2D.tm_aces.vulkan450.spv（1968 字节）需 -o 输出目录（二进制不入 stdout）
ss: fs_main.sampler2D.tm_aces.vulkan450.spv（1964 字节）需 -o 输出目录（二进制不入 stdout）
ss: vs_main.sampler2D.tm_reinhard.vulkan450.spv（1968 字节）需 -o 输出目录（二进制不入 stdout）
ss: fs_main.sampler2D.tm_reinhard.vulkan450.spv（1964 字节）需 -o 输出目录（二进制不入 stdout）
// ===== shader_spec.sampler2D.tm_aces.vulkan450.reflect.json =====
{
  "target": {"api": "vulkan", "version": 450, "explicitBinding": true, "flattenUniforms": false},
  "spec": {"TEX_KIND": "sampler2D", "TONEMAP": "tm_aces"},
  "stages": [
    {
      "name": "vs_main",
      "stage": "vert",
      "entry": "main",
      "file": "vs_main.vert",
      "inputs": [{"name": "pos", "type": "vec3", "location": 0}, {"name": "uv", "type": "vec2", "location": 1}],
      "outputs": [{"name": "clip", "builtin": "position"}, {"name": "uv", "type": "vec2", "location": 0}]
    },
    {
      "name": "fs_main",
      "stage": "frag",
      "entry": "main",
      "file": "fs_main.frag",
      "inputs": [{"name": "uv", "type": "vec2", "location": 0}],
      "outputs": [{"name": "f_color", "type": "vec4", "location": 0}]
    }
  ],
  "resources": [
    {"name": "tex", "kind": "sampler", "type": "sampler2D", "set": 0, "binding": 1},
    {"name": "Camera", "kind": "uniform", "set": 0, "binding": 0, "layout": "std140",
     "members": [{"name": "mvp", "type": "mat4", "offset": 0, "size": 64}], "size": 64}
  ]
}

// ===== shader_spec.sampler2D.tm_aces.metal20000.reflect.json =====
{
  "target": {"api": "metal", "version": 20000, "explicitBinding": true, "flattenUniforms": false},
  "spec": {"TEX_KIND": "sampler2D", "TONEMAP": "tm_aces"},
  "stages": [
    {
      "name": "vs_main",
      "stage": "vert",
      "entry": "main",
      "file": "vs_main.vert",
      "inputs": [{"name": "pos", "type": "vec3", "location": 0}, {"name": "uv", "type": "vec2", "location": 1}],
      "outputs": [{"name": "clip", "builtin": "position"}, {"name": "uv", "type": "vec2", "location": 0}]
    },
    {
      "name": "fs_main",
      "stage": "frag",
      "entry": "main",
      "file": "fs_main.frag",
      "inputs": [{"name": "uv", "type": "vec2", "location": 0}],
      "outputs": [{"name": "f_color", "type": "vec4", "location": 0}]
    }
  ],
  "resources": [
    {"name": "tex", "kind": "sampler", "type": "sampler2D", "set": 0, "binding": 1},
    {"name": "Camera", "kind": "uniform", "set": 0, "binding": 0, "layout": "std140",
     "members": [{"name": "mvp", "type": "mat4", "offset": 0, "size": 64}], "size": 64}
  ]
}

// ===== vs_main.sampler2D.tm_aces.metal20000.metal =====
#include <metal_stdlib>
#include <simd/simd.h>

using namespace metal;

struct Camera_blk
{
    float4x4 mvp;
};

struct vs_main_out
{
    float2 v_uv [[user(locn0)]];
    float4 gl_Position [[position]];
};

struct vs_main_in
{
    float3 pos [[attribute(0)]];
    float2 uv [[attribute(1)]];
};

vertex vs_main_out vs_main(vs_main_in in [[stage_in]], constant Camera_blk& Camera [[buffer(0)]])
{
    vs_main_out out = {};
    out.gl_Position = Camera.mvp * float4(in.pos, 1.0);
    out.v_uv = in.uv;
    return out;
}


// ===== fs_main.sampler2D.tm_aces.metal20000.metal =====
#pragma clang diagnostic ignored "-Wmissing-prototypes"

#include <metal_stdlib>
#include <simd/simd.h>

using namespace metal;

struct Camera_blk
{
    float4x4 mvp;
};

struct fs_main_out
{
    float4 f_color [[color(0)]];
};

struct fs_main_in
{
    float2 v_uv [[user(locn0)]];
};

static inline __attribute__((always_inline))
float3 tm_aces()
{
    float3 c;
    return (c * ((c * 2.5099999904632568359375) + float3(0.02999999932944774627685546875))) / ((c * ((c * 2.4300000667572021484375) + float3(0.589999973773956298828125))) + float3(0.14000000059604644775390625));
}

fragment fs_main_out fs_main(fs_main_in in [[stage_in]], texture2d<float> tex [[texture(0)]], sampler texSmplr [[sampler(0)]])
{
    fs_main_out out = {};
    float4 c = tex.sample(texSmplr, in.v_uv);
    out.f_color = float4(tm_aces(c.xyz), 1.0);
    return out;
}


// ===== shader_spec.sampler2D.tm_reinhard.vulkan450.reflect.json =====
{
  "target": {"api": "vulkan", "version": 450, "explicitBinding": true, "flattenUniforms": false},
  "spec": {"TEX_KIND": "sampler2D", "TONEMAP": "tm_reinhard"},
  "stages": [
    {
      "name": "vs_main",
      "stage": "vert",
      "entry": "main",
      "file": "vs_main.vert",
      "inputs": [{"name": "pos", "type": "vec3", "location": 0}, {"name": "uv", "type": "vec2", "location": 1}],
      "outputs": [{"name": "clip", "builtin": "position"}, {"name": "uv", "type": "vec2", "location": 0}]
    },
    {
      "name": "fs_main",
      "stage": "frag",
      "entry": "main",
      "file": "fs_main.frag",
      "inputs": [{"name": "uv", "type": "vec2", "location": 0}],
      "outputs": [{"name": "f_color", "type": "vec4", "location": 0}]
    }
  ],
  "resources": [
    {"name": "tex", "kind": "sampler", "type": "sampler2D", "set": 0, "binding": 1},
    {"name": "Camera", "kind": "uniform", "set": 0, "binding": 0, "layout": "std140",
     "members": [{"name": "mvp", "type": "mat4", "offset": 0, "size": 64}], "size": 64}
  ]
}

// ===== shader_spec.sampler2D.tm_reinhard.metal20000.reflect.json =====
{
  "target": {"api": "metal", "version": 20000, "explicitBinding": true, "flattenUniforms": false},
  "spec": {"TEX_KIND": "sampler2D", "TONEMAP": "tm_reinhard"},
  "stages": [
    {
      "name": "vs_main",
      "stage": "vert",
      "entry": "main",
      "file": "vs_main.vert",
      "inputs": [{"name": "pos", "type": "vec3", "location": 0}, {"name": "uv", "type": "vec2", "location": 1}],
      "outputs": [{"name": "clip", "builtin": "position"}, {"name": "uv", "type": "vec2", "location": 0}]
    },
    {
      "name": "fs_main",
      "stage": "frag",
      "entry": "main",
      "file": "fs_main.frag",
      "inputs": [{"name": "uv", "type": "vec2", "location": 0}],
      "outputs": [{"name": "f_color", "type": "vec4", "location": 0}]
    }
  ],
  "resources": [
    {"name": "tex", "kind": "sampler", "type": "sampler2D", "set": 0, "binding": 1},
    {"name": "Camera", "kind": "uniform", "set": 0, "binding": 0, "layout": "std140",
     "members": [{"name": "mvp", "type": "mat4", "offset": 0, "size": 64}], "size": 64}
  ]
}

// ===== vs_main.sampler2D.tm_reinhard.metal20000.metal =====
#include <metal_stdlib>
#include <simd/simd.h>

using namespace metal;

struct Camera_blk
{
    float4x4 mvp;
};

struct vs_main_out
{
    float2 v_uv [[user(locn0)]];
    float4 gl_Position [[position]];
};

struct vs_main_in
{
    float3 pos [[attribute(0)]];
    float2 uv [[attribute(1)]];
};

vertex vs_main_out vs_main(vs_main_in in [[stage_in]], constant Camera_blk& Camera [[buffer(0)]])
{
    vs_main_out out = {};
    out.gl_Position = Camera.mvp * float4(in.pos, 1.0);
    out.v_uv = in.uv;
    return out;
}


// ===== fs_main.sampler2D.tm_reinhard.metal20000.metal =====
#pragma clang diagnostic ignored "-Wmissing-prototypes"

#include <metal_stdlib>
#include <simd/simd.h>

using namespace metal;

struct Camera_blk
{
    float4x4 mvp;
};

struct fs_main_out
{
    float4 f_color [[color(0)]];
};

struct fs_main_in
{
    float2 v_uv [[user(locn0)]];
};

static inline __attribute__((always_inline))
float3 tm_reinhard()
{
    float3 c;
    return c / (c + float3(1.0));
}

fragment fs_main_out fs_main(fs_main_in in [[stage_in]], texture2d<float> tex [[texture(0)]], sampler texSmplr [[sampler(0)]])
{
    fs_main_out out = {};
    float4 c = tex.sample(texSmplr, in.v_uv);
    out.f_color = float4(tm_reinhard(c.xyz), 1.0);
    return out;
}
