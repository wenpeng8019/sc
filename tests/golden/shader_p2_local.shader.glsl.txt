// ===== shader_p2_local.vulkan450.reflect.json =====
{
  "target": {"api": "vulkan", "version": 450, "explicitBinding": true, "flattenUniforms": false},
  "stages": [
    {
      "name": "cs_scale",
      "stage": "comp",
      "entry": "main",
      "file": "cs_scale.comp",
      "local_size": [128, 1, 1],
      "inputs": [],
      "outputs": []
    },
    {
      "name": "cs_tile",
      "stage": "comp",
      "entry": "main",
      "file": "cs_tile.comp",
      "local_size": [8, 8, 1],
      "inputs": [],
      "outputs": []
    },
    {
      "name": "cs_default",
      "stage": "comp",
      "entry": "main",
      "file": "cs_default.comp",
      "local_size": [64, 1, 1],
      "inputs": [],
      "outputs": []
    }
  ],
  "resources": [
    {"name": "Params", "kind": "uniform", "set": 0, "binding": 0, "layout": "std140",
     "members": [{"name": "a", "type": "float", "offset": 0, "size": 4}, {"name": "n", "type": "uint", "offset": 4, "size": 4}], "size": 16},
    {"name": "XBuf", "kind": "storage", "set": 0, "binding": 1, "layout": "std430",
     "members": [{"name": "x", "type": "float", "offset": 0, "size": 4}], "size": 16}
  ]
}

// ===== cs_scale.vulkan450.comp =====
#version 450
// generated from cs_scale (comp) target vulkan450

layout(set=0, binding=0, std140) uniform Params_blk {
    float a;
    uint n;
} Params;
layout(set=0, binding=1, std430) buffer XBuf_blk {
    float x[];
} XBuf;

layout(local_size_x = 128, local_size_y = 1, local_size_z = 1) in;

void main() {
    if ((gl_GlobalInvocationID.x < Params.n)) {
        XBuf.x[gl_GlobalInvocationID.x] = (Params.a * XBuf.x[gl_GlobalInvocationID.x]);
    }
}

// ===== cs_tile.vulkan450.comp =====
#version 450
// generated from cs_tile (comp) target vulkan450

layout(set=0, binding=0, std140) uniform Params_blk {
    float a;
    uint n;
} Params;
layout(set=0, binding=1, std430) buffer XBuf_blk {
    float x[];
} XBuf;

layout(local_size_x = 8, local_size_y = 8, local_size_z = 1) in;

void main() {
    if ((gl_GlobalInvocationID.x < Params.n)) {
        XBuf.x[gl_GlobalInvocationID.x] = (XBuf.x[gl_GlobalInvocationID.x] + 1.0);
    }
}

// ===== cs_default.vulkan450.comp =====
#version 450
// generated from cs_default (comp) target vulkan450

layout(set=0, binding=0, std140) uniform Params_blk {
    float a;
    uint n;
} Params;
layout(set=0, binding=1, std430) buffer XBuf_blk {
    float x[];
} XBuf;

layout(local_size_x = 64, local_size_y = 1, local_size_z = 1) in;

void main() {
    if ((gl_GlobalInvocationID.x < Params.n)) {
        XBuf.x[gl_GlobalInvocationID.x] = (XBuf.x[gl_GlobalInvocationID.x] * 2.0);
    }
}

// ===== shader_p2_local.metal20000.reflect.json =====
{
  "target": {"api": "metal", "version": 20000, "explicitBinding": true, "flattenUniforms": false},
  "stages": [
    {
      "name": "cs_scale",
      "stage": "comp",
      "entry": "main",
      "file": "cs_scale.comp",
      "local_size": [128, 1, 1],
      "inputs": [],
      "outputs": []
    },
    {
      "name": "cs_tile",
      "stage": "comp",
      "entry": "main",
      "file": "cs_tile.comp",
      "local_size": [8, 8, 1],
      "inputs": [],
      "outputs": []
    },
    {
      "name": "cs_default",
      "stage": "comp",
      "entry": "main",
      "file": "cs_default.comp",
      "local_size": [64, 1, 1],
      "inputs": [],
      "outputs": []
    }
  ],
  "resources": [
    {"name": "Params", "kind": "uniform", "set": 0, "binding": 0, "layout": "std140",
     "members": [{"name": "a", "type": "float", "offset": 0, "size": 4}, {"name": "n", "type": "uint", "offset": 4, "size": 4}], "size": 16},
    {"name": "XBuf", "kind": "storage", "set": 0, "binding": 1, "layout": "std430",
     "members": [{"name": "x", "type": "float", "offset": 0, "size": 4}], "size": 16}
  ]
}

// ===== cs_scale.metal20000.comp =====
#version 450
// generated from cs_scale (comp) target vulkan450

layout(set=0, binding=0, std140) uniform Params_blk {
    float a;
    uint n;
} Params;
layout(set=0, binding=1, std430) buffer XBuf_blk {
    float x[];
} XBuf;

layout(local_size_x = 128, local_size_y = 1, local_size_z = 1) in;

void main() {
    if ((gl_GlobalInvocationID.x < Params.n)) {
        XBuf.x[gl_GlobalInvocationID.x] = (Params.a * XBuf.x[gl_GlobalInvocationID.x]);
    }
}

// ===== cs_tile.metal20000.comp =====
#version 450
// generated from cs_tile (comp) target vulkan450

layout(set=0, binding=0, std140) uniform Params_blk {
    float a;
    uint n;
} Params;
layout(set=0, binding=1, std430) buffer XBuf_blk {
    float x[];
} XBuf;

layout(local_size_x = 8, local_size_y = 8, local_size_z = 1) in;

void main() {
    if ((gl_GlobalInvocationID.x < Params.n)) {
        XBuf.x[gl_GlobalInvocationID.x] = (XBuf.x[gl_GlobalInvocationID.x] + 1.0);
    }
}

// ===== cs_default.metal20000.comp =====
#version 450
// generated from cs_default (comp) target vulkan450

layout(set=0, binding=0, std140) uniform Params_blk {
    float a;
    uint n;
} Params;
layout(set=0, binding=1, std430) buffer XBuf_blk {
    float x[];
} XBuf;

layout(local_size_x = 64, local_size_y = 1, local_size_z = 1) in;

void main() {
    if ((gl_GlobalInvocationID.x < Params.n)) {
        XBuf.x[gl_GlobalInvocationID.x] = (XBuf.x[gl_GlobalInvocationID.x] * 2.0);
    }
}

// ===== shader_p2_local.glcore430.reflect.json =====
{
  "target": {"api": "glcore", "version": 430, "explicitBinding": true, "flattenUniforms": false},
  "stages": [
    {
      "name": "cs_scale",
      "stage": "comp",
      "entry": "main",
      "file": "cs_scale.comp",
      "local_size": [128, 1, 1],
      "inputs": [],
      "outputs": []
    },
    {
      "name": "cs_tile",
      "stage": "comp",
      "entry": "main",
      "file": "cs_tile.comp",
      "local_size": [8, 8, 1],
      "inputs": [],
      "outputs": []
    },
    {
      "name": "cs_default",
      "stage": "comp",
      "entry": "main",
      "file": "cs_default.comp",
      "local_size": [64, 1, 1],
      "inputs": [],
      "outputs": []
    }
  ],
  "resources": [
    {"name": "Params", "kind": "uniform", "set": -1, "binding": 0, "layout": "std140",
     "members": [{"name": "a", "type": "float", "offset": 0, "size": 4}, {"name": "n", "type": "uint", "offset": 4, "size": 4}], "size": 16},
    {"name": "XBuf", "kind": "storage", "set": -1, "binding": 1, "layout": "std430",
     "members": [{"name": "x", "type": "float", "offset": 0, "size": 4}], "size": 16}
  ]
}

// ===== cs_scale.glcore430.comp =====
#version 430 core
// generated from cs_scale (comp) target glcore430

layout(binding=0, std140) uniform Params_blk {
    float a;
    uint n;
} Params;
layout(binding=1, std430) buffer XBuf_blk {
    float x[];
} XBuf;

layout(local_size_x = 128, local_size_y = 1, local_size_z = 1) in;

void main() {
    if ((gl_GlobalInvocationID.x < Params.n)) {
        XBuf.x[gl_GlobalInvocationID.x] = (Params.a * XBuf.x[gl_GlobalInvocationID.x]);
    }
}

// ===== cs_tile.glcore430.comp =====
#version 430 core
// generated from cs_tile (comp) target glcore430

layout(binding=0, std140) uniform Params_blk {
    float a;
    uint n;
} Params;
layout(binding=1, std430) buffer XBuf_blk {
    float x[];
} XBuf;

layout(local_size_x = 8, local_size_y = 8, local_size_z = 1) in;

void main() {
    if ((gl_GlobalInvocationID.x < Params.n)) {
        XBuf.x[gl_GlobalInvocationID.x] = (XBuf.x[gl_GlobalInvocationID.x] + 1.0);
    }
}

// ===== cs_default.glcore430.comp =====
#version 430 core
// generated from cs_default (comp) target glcore430

layout(binding=0, std140) uniform Params_blk {
    float a;
    uint n;
} Params;
layout(binding=1, std430) buffer XBuf_blk {
    float x[];
} XBuf;

layout(local_size_x = 64, local_size_y = 1, local_size_z = 1) in;

void main() {
    if ((gl_GlobalInvocationID.x < Params.n)) {
        XBuf.x[gl_GlobalInvocationID.x] = (XBuf.x[gl_GlobalInvocationID.x] * 2.0);
    }
}
