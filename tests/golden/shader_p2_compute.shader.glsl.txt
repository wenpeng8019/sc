// ===== shader_p2_compute.vulkan450.reflect.json =====
{
  "target": {"api": "vulkan", "version": 450, "explicitBinding": true, "flattenUniforms": false},
  "stages": [
    {
      "name": "cs_reduce",
      "stage": "comp",
      "entry": "main",
      "file": "cs_reduce.comp",
      "local_size": [256, 1, 1],
      "inputs": [],
      "outputs": []
    }
  ],
  "resources": [
    {"name": "Params", "kind": "uniform", "set": 0, "binding": 0, "layout": "std140",
     "members": [{"name": "n", "type": "uint", "offset": 0, "size": 4}], "size": 16},
    {"name": "XBuf", "kind": "storage", "set": 0, "binding": 1, "layout": "std430",
     "members": [{"name": "x", "type": "float", "offset": 0, "size": 4}], "size": 16},
    {"name": "OutBuf", "kind": "storage", "set": 0, "binding": 2, "layout": "std430",
     "members": [{"name": "total", "type": "uint", "offset": 0, "size": 4}, {"name": "hist", "type": "uint", "offset": 4, "size": 64}], "size": 80}
  ]
}

// ===== cs_reduce.vulkan450.comp =====
#version 450
// generated from cs_reduce (comp) target vulkan450

layout(set=0, binding=0, std140) uniform Params_blk {
    uint n;
} Params;
layout(set=0, binding=1, std430) buffer XBuf_blk {
    float x[];
} XBuf;
layout(set=0, binding=2, std430) buffer OutBuf_blk {
    uint total;
    uint hist[16];
} OutBuf;
struct Tile_blk {
    float data[256];
};
shared Tile_blk Tile;

layout(local_size_x = 256, local_size_y = 1, local_size_z = 1) in;

void main() {
    if ((gl_GlobalInvocationID.x < Params.n)) {
        Tile.data[gl_LocalInvocationIndex] = XBuf.x[gl_GlobalInvocationID.x];
    } else {
        Tile.data[gl_LocalInvocationIndex] = 0.0;
    }
    barrier();
    uint stride = 128;
    while ((stride > 0)) {
        if ((gl_LocalInvocationIndex < stride)) {
            Tile.data[gl_LocalInvocationIndex] = (Tile.data[gl_LocalInvocationIndex] + Tile.data[(gl_LocalInvocationIndex + stride)]);
        }
        barrier();
        stride = (stride / 2);
    }
    if ((gl_LocalInvocationIndex == 0)) {
        atomicAdd(OutBuf.total, uint(Tile.data[0]));
        atomicMax(OutBuf.hist[0], uint(Tile.data[0]));
        atomicMin(OutBuf.hist[1], uint(Tile.data[0]));
        atomicOr(OutBuf.hist[2], 1);
        atomicAnd(OutBuf.hist[3], 255);
        atomicXor(OutBuf.hist[4], gl_GlobalInvocationID.x);
        atomicAdd(OutBuf.hist[5], -(1));
        atomicExchange(OutBuf.hist[6], gl_GlobalInvocationID.x);
        atomicCompSwap(OutBuf.hist[7], 0, 7);
    }
    memoryBarrier();
}

// ===== shader_p2_compute.metal20000.reflect.json =====
{
  "target": {"api": "metal", "version": 20000, "explicitBinding": true, "flattenUniforms": false},
  "stages": [
    {
      "name": "cs_reduce",
      "stage": "comp",
      "entry": "main",
      "file": "cs_reduce.comp",
      "local_size": [256, 1, 1],
      "inputs": [],
      "outputs": []
    }
  ],
  "resources": [
    {"name": "Params", "kind": "uniform", "set": 0, "binding": 0, "layout": "std140",
     "members": [{"name": "n", "type": "uint", "offset": 0, "size": 4}], "size": 16},
    {"name": "XBuf", "kind": "storage", "set": 0, "binding": 1, "layout": "std430",
     "members": [{"name": "x", "type": "float", "offset": 0, "size": 4}], "size": 16},
    {"name": "OutBuf", "kind": "storage", "set": 0, "binding": 2, "layout": "std430",
     "members": [{"name": "total", "type": "uint", "offset": 0, "size": 4}, {"name": "hist", "type": "uint", "offset": 4, "size": 64}], "size": 80}
  ]
}

// ===== cs_reduce.metal20000.comp =====
#version 450
// generated from cs_reduce (comp) target vulkan450

layout(set=0, binding=0, std140) uniform Params_blk {
    uint n;
} Params;
layout(set=0, binding=1, std430) buffer XBuf_blk {
    float x[];
} XBuf;
layout(set=0, binding=2, std430) buffer OutBuf_blk {
    uint total;
    uint hist[16];
} OutBuf;
struct Tile_blk {
    float data[256];
};
shared Tile_blk Tile;

layout(local_size_x = 256, local_size_y = 1, local_size_z = 1) in;

void main() {
    if ((gl_GlobalInvocationID.x < Params.n)) {
        Tile.data[gl_LocalInvocationIndex] = XBuf.x[gl_GlobalInvocationID.x];
    } else {
        Tile.data[gl_LocalInvocationIndex] = 0.0;
    }
    barrier();
    uint stride = 128;
    while ((stride > 0)) {
        if ((gl_LocalInvocationIndex < stride)) {
            Tile.data[gl_LocalInvocationIndex] = (Tile.data[gl_LocalInvocationIndex] + Tile.data[(gl_LocalInvocationIndex + stride)]);
        }
        barrier();
        stride = (stride / 2);
    }
    if ((gl_LocalInvocationIndex == 0)) {
        atomicAdd(OutBuf.total, uint(Tile.data[0]));
        atomicMax(OutBuf.hist[0], uint(Tile.data[0]));
        atomicMin(OutBuf.hist[1], uint(Tile.data[0]));
        atomicOr(OutBuf.hist[2], 1);
        atomicAnd(OutBuf.hist[3], 255);
        atomicXor(OutBuf.hist[4], gl_GlobalInvocationID.x);
        atomicAdd(OutBuf.hist[5], -(1));
        atomicExchange(OutBuf.hist[6], gl_GlobalInvocationID.x);
        atomicCompSwap(OutBuf.hist[7], 0, 7);
    }
    memoryBarrier();
}

// ===== shader_p2_compute.gles310.reflect.json =====
{
  "target": {"api": "gles", "version": 310, "explicitBinding": true, "flattenUniforms": false},
  "stages": [
    {
      "name": "cs_reduce",
      "stage": "comp",
      "entry": "main",
      "file": "cs_reduce.comp",
      "local_size": [256, 1, 1],
      "inputs": [],
      "outputs": []
    }
  ],
  "resources": [
    {"name": "Params", "kind": "uniform", "set": -1, "binding": 0, "layout": "std140",
     "members": [{"name": "n", "type": "uint", "offset": 0, "size": 4}], "size": 16},
    {"name": "XBuf", "kind": "storage", "set": -1, "binding": 1, "layout": "std430",
     "members": [{"name": "x", "type": "float", "offset": 0, "size": 4}], "size": 16},
    {"name": "OutBuf", "kind": "storage", "set": -1, "binding": 2, "layout": "std430",
     "members": [{"name": "total", "type": "uint", "offset": 0, "size": 4}, {"name": "hist", "type": "uint", "offset": 4, "size": 64}], "size": 80}
  ]
}

// ===== cs_reduce.gles310.comp =====
#version 310 es
// generated from cs_reduce (comp) target gles310

precision highp float;
precision highp int;

layout(binding=0, std140) uniform Params_blk {
    uint n;
} Params;
layout(binding=1, std430) buffer XBuf_blk {
    float x[];
} XBuf;
layout(binding=2, std430) buffer OutBuf_blk {
    uint total;
    uint hist[16];
} OutBuf;
struct Tile_blk {
    float data[256];
};
shared Tile_blk Tile;

layout(local_size_x = 256, local_size_y = 1, local_size_z = 1) in;

void main() {
    if ((gl_GlobalInvocationID.x < Params.n)) {
        Tile.data[gl_LocalInvocationIndex] = XBuf.x[gl_GlobalInvocationID.x];
    } else {
        Tile.data[gl_LocalInvocationIndex] = 0.0;
    }
    barrier();
    uint stride = 128;
    while ((stride > 0)) {
        if ((gl_LocalInvocationIndex < stride)) {
            Tile.data[gl_LocalInvocationIndex] = (Tile.data[gl_LocalInvocationIndex] + Tile.data[(gl_LocalInvocationIndex + stride)]);
        }
        barrier();
        stride = (stride / 2);
    }
    if ((gl_LocalInvocationIndex == 0)) {
        atomicAdd(OutBuf.total, uint(Tile.data[0]));
        atomicMax(OutBuf.hist[0], uint(Tile.data[0]));
        atomicMin(OutBuf.hist[1], uint(Tile.data[0]));
        atomicOr(OutBuf.hist[2], 1);
        atomicAnd(OutBuf.hist[3], 255);
        atomicXor(OutBuf.hist[4], gl_GlobalInvocationID.x);
        atomicAdd(OutBuf.hist[5], -(1));
        atomicExchange(OutBuf.hist[6], gl_GlobalInvocationID.x);
        atomicCompSwap(OutBuf.hist[7], 0, 7);
    }
    memoryBarrier();
}
