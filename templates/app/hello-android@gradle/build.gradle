// hello-android@gradle —— 根工程 build.gradle
//
// 仅声明 Android Gradle Plugin（AGP）版本；实际配置在 app/build.gradle。
// apply false = 根工程不套用，交给子模块 :app 套用。
plugins {
    id 'com.android.application' version '8.5.0' apply false
}
